"use client" //for interactivity(like states)
import { useState } from "react"

const NavItems = () => {
  const [activeIndex, setActiveIndex] = useState<null | Number>(null)
  return(
    <div></div>
  )
}
export default NavItems
